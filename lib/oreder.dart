import 'package:flutter/material.dart';

class CafeOreder extends StatelessWidget {
  const CafeOreder({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
