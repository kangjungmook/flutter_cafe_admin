import 'package:flutter/material.dart';

class CafeOrder extends StatelessWidget {
  const CafeOrder({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
